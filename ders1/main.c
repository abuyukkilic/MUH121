#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	printf("Hello World");
	//printf("Merhaba Dünya"); // Türkçe karakter problemi mevcut.
	getch(); //Klavyeden bir tuşa basma
	getchar(); // Klavyeden tuş ve entera basma
	
	
	int x; //Değişken isimlendirme kurallarına uymalıdır. (Sayı ile başlama, rezerve edilmiş kelimeler ...)
	x=3;
	
	
	
	
	return 0;
}